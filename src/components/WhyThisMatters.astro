---
interface Props { text: string; label?: string; }
const { text, label = "Why this matters" } = Astro.props;
---
<aside class="notice notice--matters" aria-label={label}>
  <strong>{label}:</strong> {text}
</aside>
